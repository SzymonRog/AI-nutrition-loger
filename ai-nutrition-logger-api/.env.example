# Supabase
SUPABASE_URL=https://pwszfflxgmnehiftetww.supabase.co
# Service-role / secret key (SECRET — backend only, never expose to the frontend).
# Supabase dashboard → Settings → API Keys → reveal the secret (sb_secret_...) key.
SUPABASE_SERVICE_ROLE_KEY=

# Third-party APIs
GOOGLE_API_KEY=
USDA_API_KEY=

# Comma-separated list of allowed frontend origins for CORS.
CORS_ORIGINS=http://localhost:5173
